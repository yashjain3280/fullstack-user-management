# 🧩 Full Stack User Management Application

A **CRUD (Create, Read, Update, Delete)** web application built using **Spring Boot**, **React.js**, and **MySQL**.  
This project demonstrates how a frontend (React) and backend (Spring Boot) communicate via RESTful APIs to manage user data stored in a MySQL database.

---

## 🚀 Features

✅ Add new users  
✅ View all users  
✅ Edit user details  
✅ Delete existing users  
✅ Responsive UI built with Bootstrap  
✅ Backend REST APIs using Spring Boot & JPA  
✅ Database integration with MySQL  
✅ Seamless communication between frontend & backend using Axios  

---

## ⚙️ Tech Stack

### 🖥️ Frontend
- React.js (with Hooks)
- Axios (for API calls)
- React Router (for navigation)
- Bootstrap (for styling)

### ⚙️ Backend
- Spring Boot
- Spring Data JPA
- Hibernate ORM
- MySQL Database
- Maven Build Tool

---
Setup for backend Springtboot application.properties
spring.datasource.url=jdbc:mysql://localhost:3306/userdb
spring.datasource.username=root
spring.datasource.password=YOUR_MYSQL_PASSWORD
spring.jpa.hibernate.ddl-auto=update
## 🧱 Project Structure

